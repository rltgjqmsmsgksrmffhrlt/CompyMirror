# 웹캠 거울 앱 기획 문서

---

## 문서 이력

| 버전 | 날짜 | 작성자 | 변경 내용 |
|------|------|--------|-----------|
| v1.0 | 2025-05-17 | - | 최초 작성 — 기능 정의, 보안 정책, 필터 스펙, 구현 가이드 포함 |

---

## 목차

1. [개요](#1-개요)
2. [보안 정책](#2-보안-정책)
3. [핵심 기능](#3-핵심-기능)
4. [뷰티 필터 스펙](#4-뷰티-필터-스펙)
5. [프리셋 정의](#5-프리셋-정의)
6. [UI 구성](#6-ui-구성)
7. [기술 스택 및 구현 가이드](#7-기술-스택-및-구현-가이드)
8. [해상도 처리](#8-해상도-처리)
9. [향후 개선 아이디어](#9-향후-개선-아이디어)

---

## 1. 개요

컴퓨터 내장 웹캠(또는 외장 카메라)을 실시간 거울처럼 사용하는 웹 앱.
별도 설치 없이 브라우저에서 바로 실행되며, 뷰티 필터와 스냅샷 기능을 제공한다.

**목표 사용자:** PC/Mac 사용자 중 거울 대용으로 카메라를 활용하고 싶은 사람

**핵심 가치:**
- 설치 불필요 (브라우저 기반)
- 강력한 프라이버시 보장 (로컬 처리 전용)
- 뷰티 앱 수준의 필터 제공

---

## 2. 보안 정책

> **이 앱의 가장 중요한 차별점은 보안이다.**

### 2.1 로컬 전용 처리 원칙

- 카메라 영상은 **서버로 전송되지 않는다**
- 모든 픽셀 연산은 **브라우저 내 Canvas API** 에서 수행
- 네트워크 요청 없음 — 완전한 오프라인 동작 가능
- 스냅샷 저장도 로컬 다운로드만 허용

### 2.2 권한 처리

- 카메라 권한은 브라우저 표준 `getUserMedia` API를 통해 사용자 명시 동의 후 획득
- 권한 거부 시 앱이 동작하지 않으며, 강제 접근하지 않음
- 페이지 이탈 또는 "끄기" 버튼 클릭 시 즉시 스트림 종료 (`MediaStreamTrack.stop()`)

### 2.3 UI에서 보안 강조

- 화면 상단에 항상 보안 배지 표시
- 문구 예시: **"로컬 전용 — 카메라 영상이 서버로 전송되지 않습니다"**
- 색상: 초록 계열로 신뢰감 표시

---

## 3. 핵심 기능

| 기능 | 설명 |
|------|------|
| 실시간 거울 | 카메라 영상을 좌우 반전하여 거울처럼 표시 |
| 좌우 반전 토글 | 반전 on/off 버튼으로 전환 |
| 뷰티 필터 | 밝기·대비·채도·따뜻함·피부보정 슬라이더 |
| 프리셋 | 자주 쓰는 필터 조합을 원클릭으로 적용 |
| 스냅샷 | 현재 화면(필터 적용 상태)을 이미지로 저장 |
| 카메라 on/off | 사용 후 즉시 스트림 종료 가능 |

---

## 4. 뷰티 필터 스펙

모든 필터는 **Canvas `getImageData` / `putImageData`** 를 이용한 픽셀 직접 연산으로 구현.
CSS filter는 사용하지 않는다 (픽셀 데이터 접근과 충돌하므로).

### 4.1 밝기 (Brightness)

```
범위: -100 ~ +100  (기본값: 0)
연산: R' = clamp(R + bright)
      G' = clamp(G + bright)
      B' = clamp(B + bright)
```

### 4.2 대비 (Contrast)

```
범위: -100 ~ +100  (기본값: 0)
연산: factor = (259 × (contrast + 255)) / (255 × (259 - contrast))
      R' = clamp(factor × (R - 128) + 128)
```

### 4.3 채도 (Saturation)

```
범위: -100 ~ +100  (기본값: 0)
      -100이면 흑백, 0이면 원본, +100이면 채도 2배
연산: gray = 0.299R + 0.587G + 0.114B
      R' = clamp(gray + (R - gray) × sFactor)
      sFactor = (sat + 100) / 100
```

### 4.4 따뜻함 (Warmth)

```
범위: -80 ~ +80  (기본값: 0)
      양수: 웜톤(붉은 계열), 음수: 쿨톤(파란 계열)
연산: R' = clamp(R + warm × 1.2)
      B' = clamp(B - warm × 1.0)
```

### 4.5 피부보정 (Skin Smoothing)

```
범위: 0 ~ 8  (기본값: 0, 단위: blur radius px)
연산: 가로 방향 박스 블러 (Box Blur)
      반경 r 내 픽셀 평균값으로 대체
      성능 고려 시 r ≤ 4 권장
```

---

## 5. 프리셋 정의

| 프리셋 | 밝기 | 대비 | 채도 | 따뜻함 | 피부보정 | 용도 |
|--------|------|------|------|--------|----------|------|
| 없음   |  0   |  0   |  0   |   0    |    0     | 원본 |
| 내추럴 | +15  | +5   | +10  |  +15   |    2     | 자연스러운 보정 |
| 글로우 | +35  | -15  | +5   |  +20   |    4     | 밝고 부드러운 피부 |
| 비비드 | +5   | +40  | +70  |   0    |    1     | 선명하고 강렬한 색감 |
| 흑백   |  0   | +20  | -100 |   0    |    1     | 모노크롬 |
| 웜톤   | +10  | +10  | +20  |  +60   |    2     | 따뜻하고 아늑한 톤 |
| 쿨톤   | +5   | +10  | +10  |  -60   |    2     | 차갑고 세련된 톤 |

---

## 6. UI 구성

```
┌─────────────────────────────┐
│  🛡 로컬 전용 보안 배지      │
├─────────────────────────────┤
│                             │
│      카메라 뷰파인더         │
│      (max-width: 420px)     │
│      aspect-ratio: 4:3      │
│                             │
├─────────────────────────────┤
│  [시작] [끄기] [반전] [스냅] │
├─────────────────────────────┤
│  프리셋 버튼 (7종)           │
├─────────────────────────────┤
│  슬라이더 (5종)              │
│  - 밝기 / 대비 / 채도        │
│  - 따뜻함 / 피부보정         │
├─────────────────────────────┤
│  스냅샷 미리보기 (선택)       │
└─────────────────────────────┘
```

### 레이아웃 규칙

- 뷰파인더: `max-width: 420px`, `aspect-ratio: 4/3`
- 뷰파인더 내부: `<canvas>` 가 video 위에 절대 위치로 오버레이
- 필터 슬라이더: label(65px 고정) + range(flex:1) + 숫자값(28px 고정)
- 프리셋 버튼: 선택된 항목은 info 색상으로 강조

---

## 7. 기술 스택 및 구현 가이드

### 7.1 권장 스택

| 항목 | 선택 |
|------|------|
| 런타임 | 순수 HTML/JS (프레임워크 불필요) |
| 카메라 API | `navigator.mediaDevices.getUserMedia` |
| 렌더링 | `Canvas 2D API` |
| 저장 | `canvas.toDataURL()` + `<a download>` |

### 7.2 카메라 시작 코드 패턴

```js
const stream = await navigator.mediaDevices.getUserMedia({
  video: {
    width: { ideal: 1920 },
    height: { ideal: 1080 },
    facingMode: 'user'
  },
  audio: false
});
video.srcObject = stream;
```

### 7.3 렌더링 루프

```js
function loop() {
  ctx.save();
  if (flipped) { ctx.translate(videoWidth, 0); ctx.scale(-1, 1); }
  ctx.drawImage(video, 0, 0, w, h);
  ctx.restore();

  // 픽셀 연산 필터 적용
  const imgd = ctx.getImageData(0, 0, w, h);
  processPixels(imgd.data, w, h);
  ctx.putImageData(imgd, 0, 0);

  requestAnimationFrame(loop);
}
```

### 7.4 스트림 종료

```js
stream.getTracks().forEach(track => track.stop());
```

> **주의:** 페이지 언로드(`beforeunload`) 이벤트에서도 반드시 종료 처리할 것

### 7.5 스냅샷 저장

```js
const link = document.createElement('a');
link.download = `mirror_${Date.now()}.png`;
link.href = canvas.toDataURL('image/png');
link.click();
```

---

## 8. 해상도 처리

- `getUserMedia`에서 `ideal: 1920×1080` 요청하지만, **실제 해상도는 하드웨어에 따라 결정됨**
- 내장 노트북 카메라는 대부분 720p(1280×720) 이하
- Canvas는 `video.videoWidth / video.videoHeight` 기준으로 동적 크기 설정
- 표시 크기(CSS)와 실제 해상도(canvas width/height)를 분리하여 선명도 유지

```js
// 해상도 동적 반영
if (canvas.width !== video.videoWidth) {
  canvas.width = video.videoWidth;
  canvas.height = video.videoHeight;
}
```

---

## 9. 향후 개선 아이디어

| 아이디어 | 난이도 | 설명 |
|----------|--------|------|
| 선명도 필터 | 중 | 언샤프마스크(Unsharp Mask) 알고리즘 적용 |
| 얼굴 인식 보정 | 고 | MediaPipe Face Mesh로 얼굴 영역만 피부보정 |
| 전신 모드 | 하 | 뷰파인더 aspect-ratio를 9:16으로 전환하는 버튼 추가 |
| 필터 저장 | 하 | localStorage에 마지막 슬라이더 값 저장 |
| 배경 블러 | 고 | MediaPipe Selfie Segmentation으로 인물만 선명하게 |
| 타이머 스냅샷 | 하 | 3/5/10초 카운트다운 후 자동 촬영 |
| 다중 카메라 | 중 | 외장 웹캠 연결 시 카메라 전환 기능 |
